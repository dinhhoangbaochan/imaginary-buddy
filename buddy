<?php

require 'commands.php';

$console = new Console();

$console->handle_min_argument( $argc );
$console->handle_invalid_argument( $argc, $argv[1] );
// $console->make($argv[1]);

// $command = $argv[1];

var_dump(explode(":", $argv[1]));